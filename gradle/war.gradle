apply plugin: "war"

bootWar {
    mainClass = "io.github.erp.CalvaryApiGatewayApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
